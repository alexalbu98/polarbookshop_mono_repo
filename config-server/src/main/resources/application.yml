spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alexalbu98/polarbookshop_mono_repo.git
          search-paths: config-server/configs
          timeout: 5
          clone-on-start: true
          force-pull: true
          default-label: develop

server:
  port: 8888
